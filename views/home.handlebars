{{#if posts.length}}
  <ul>
    {{#each posts}}
      <li>
        <h2>{{title}}</h2>
        <p>{{content}}</p>
        <p>Posted by: {{userData.name}}</p>
        <!-- Display comments and other post-related data -->
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No posts found.</p>
{{/if}}